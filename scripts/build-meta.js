const fs=require('fs');const path=require('path');const root=process.cwd();const site=process.env.VITE_SITE_URL||'https://euphoria.example';const stamp=(p)=>{let s=fs.readFileSync(p,'utf8');s=s.replace(/%VITE_SITE_URL%/g,site);fs.writeFileSync(p,s)};stamp(path.join(root,'index.html'));stamp(path.join(root,'public','robots.txt'));stamp(path.join(root,'public','sitemap.xml'));console.log('Stamped meta with SITE_URL:',site);